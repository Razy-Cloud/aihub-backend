# ===== AIHub 部署配置 =====

# Render.com 部署配置
# 在 Render.com 连接 GitHub 仓库后自动识别

# 服务类型：Web Service
# 构建命令：npm install
# 启动命令：npm start
# 环境变量：在 Render 控制台手动添加

# ===== 环境变量（在 Render 控制台设置）=====
# PORT=10000 (Render 自动设置)
# JWT_SECRET=<随机字符串>
# DEEPSEEK_API_KEY=<你的Key>
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
# QWEN_API_KEY=<可选>
# OPENAI_API_KEY=<可选>
